import React from 'react';
import ReactDOM from 'react-dom/client';
import CrewManagementApp from './components/CrewManagementApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CrewManagementApp />
  </React.StrictMode>,
);